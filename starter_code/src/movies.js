/* eslint no-restricted-globals: 'off' */
// Turn duration of the movies from hours to minutes 

function turnHoursToMinutes(obj, key) {

  return obj.map(function (elm){

  } 
  )}


/*
----------------------------------------------------------------------------------------

Estuve haciendo testeos para hacer un cambio de strings a num para su posterior inclusión en el array.

----------------------------------------------------------------------------------------

 var string = "2 hours 10 mins"


var stringtoarray = string.split('hours')


function hourstominutes() {
  var hours
  var mins
  
  if (string.indexOf('hours') !== -1){
    stringtoarray = string.split('hours')
    hours = parseFloat(stringtoarray[0]) * 60
      if (string.indexOf('m') !== -1) {
        mins = parseFloat(stringtoarray[1])
        
    } else {
      mins = 0
    }
  } else if (string.indexOf('m') !== -1) {
    mins = parseFloat(stringtoarray[1])
  } else {
    mins = 0
  
  }

  return hours + mins

}

hourstominutes()

*/
  

  

// Get the average of all rates with 2 decimals 

function ratesAverage() {}

// Get the average of Drama Movies
function dramaMoviesRate() {

  return 
}

// Order by time duration, in growing order


// How many movies did STEVEN SPIELBERG


// Order by title and print the first 20 titles


// Best yearly rate average



// return Object.assign({}, movies.duration, {duration: movies.map(function (elm){    })} 

//  .parseInt(obj.key)//